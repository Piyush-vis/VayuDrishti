# Base URL of the VayuDrishti FastAPI backend. Optional - defaults to
# http://localhost:8000/api/v1 if unset, which matches the default docker-compose
# and manual local setup.
VITE_API_BASE_URL=http://localhost:8000/api/v1
